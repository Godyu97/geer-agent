[package]
name = "geer-agent"
version = "0.1.0"
edition = "2024"

[features]
embed-env = []

[dependencies]
async-openai = { version = "0.42.0", features = ["chat-completion", "responses"] }
dotenvy = "0.15"
futures-util = "0.3"
tokio = { version = "1", features = ["macros", "rt", "time"] }
